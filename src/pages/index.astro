---
// src/pages/index.astro
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import ParkingImage from '../components/ParkingImage.astro';
import ParkingButtons from '../components/ParkingButtons.astro';
import ReservationModal from '../components/ReservationModal.astro';
import Footer from '../components/Footer.astro';
---

<Layout title="ParkSpace - Sistema de Reservas de Estacionamiento">
  <Header />
  
  <main class="min-h-screen py-8">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <!-- Hero Section -->
      <div class="text-center mb-12">
        <h1 class="text-4xl md:text-5xl font-bold text-white mb-4">
          Reserva tu <span class="text-blue-400">Espacio de Estacionamiento</span>
        </h1>
        <p class="text-xl text-gray-300 mb-8 max-w-3xl mx-auto">
          Sistema profesional de reservas con espacios seguros, techados y disponibles 24/7. 
          Selecciona tu espacio, elige los días y reserva fácilmente por WhatsApp.
        </p>
          
      <!-- Plano del estacionamiento -->
      <ParkingImage />
      
      <!-- Botones de selección -->
      <ParkingButtons />
      
      <!-- Información adicional -->
      <div class="grid grid-cols-1 lg:grid-cols-2 gap-8 mb-12">
        <!-- Precios -->
        <div class="bg-gray-800 p-6 rounded-lg border border-gray-700 shadow-lg">
          <h3 class="text-xl font-bold text-white mb-4 flex items-center">
            <svg class="w-6 h-6 mr-2 text-green-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1"></path>
            </svg>
            Tarifas y Combos
          </h3>
          <div class="space-y-3">
            <div class="flex justify-between items-center p-3 bg-gray-700 rounded">
              <span class="text-gray-200">1 día</span>
              <span class="text-green-400 font-bold">$50</span>
            </div>
            <div class="flex justify-between items-center p-3 bg-gray-700 rounded">
              <span class="text-gray-200">2 días</span>
              <span class="text-green-400 font-bold">$90 <span class="text-xs text-gray-400 ml-1">(-$10)</span></span>
            </div>
            <div class="flex justify-between items-center p-3 bg-gray-700 rounded">
              <span class="text-gray-200">3 días</span>
              <span class="text-green-400 font-bold">$120 <span class="text-xs text-gray-400 ml-1">(-$30)</span></span>
            </div>
            <div class="flex justify-between items-center p-3 bg-gray-700 rounded">
              <span class="text-gray-200">4 días</span>
              <span class="text-green-400 font-bold">$150 <span class="text-xs text-gray-400 ml-1">(-$50)</span></span>
            </div>
            <div class="flex justify-between items-center p-3 bg-gradient-to-r from-blue-700 to-blue-600 rounded border border-blue-500">
              <span class="text-white font-semibold">5 días (Combo Semanal)</span>
              <span class="text-yellow-300 font-bold">$175 <span class="text-xs text-blue-200 ml-1">(-$75)</span></span>
            </div>
          </div>
        </div>
        
        <!-- Instrucciones -->
        <div class="bg-gray-800 p-6 rounded-lg border border-gray-700 shadow-lg">
          <h3 class="text-xl font-bold text-white mb-4 flex items-center">
            <svg class="w-6 h-6 mr-2 text-blue-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"></path>
            </svg>
            Cómo Reservar
          </h3>
          <ol class="space-y-3 text-gray-300">
            <li class="flex items-start">
              <span class="bg-blue-600 text-white rounded-full w-6 h-6 flex items-center justify-center text-sm font-bold mr-3 mt-0.5">1</span>
              Observa el plano y selecciona tu espacio preferido
            </li>
            <li class="flex items-start">
              <span class="bg-blue-600 text-white rounded-full w-6 h-6 flex items-center justify-center text-sm font-bold mr-3 mt-0.5">2</span>
              Haz clic en el número del espacio que deseas
            </li>
            <li class="flex items-start">
              <span class="bg-blue-600 text-white rounded-full w-6 h-6 flex items-center justify-center text-sm font-bold mr-3 mt-0.5">3</span>
              Selecciona la cantidad de días (1-5 días)
            </li>
            <li class="flex items-start">
              <span class="bg-blue-600 text-white rounded-full w-6 h-6 flex items-center justify-center text-sm font-bold mr-3 mt-0.5">4</span>
              Confirma tu reserva por WhatsApp
            </li>
          </ol>
          
          <div class="mt-6 p-4 bg-green-700 bg-opacity-30 border border-green-600 rounded-lg">
            <p class="text-green-200 text-sm">
              <strong>¡Nota importante!</strong> Los precios incluyen descuentos automáticos por reservas de múltiples días. 
              Mientras más días reserves, mayor será tu ahorro.
            </p>
          </div>
        </div>
      </div>
    </div>
  </main>
  
  <!-- Modal de reservación -->
  <ReservationModal />
  
  <!-- Footer -->
  <Footer />
</Layout>

<style>
  /* Animaciones suaves */
  @keyframes fadeIn {
    from { opacity: 0; transform: translateY(20px); }
    to { opacity: 1; transform: translateY(0); }
  }
  
  main {
    animation: fadeIn 0.6s ease-out;
  }
  
  /* Efecto hover para las tarjetas de características */
  .bg-gray-800:hover {
    transform: translateY(-2px);
    transition: transform 0.3s ease;
  }
</style>